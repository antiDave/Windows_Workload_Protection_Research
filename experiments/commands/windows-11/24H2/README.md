# Windows 11 24H2

Use this command pack for Windows 11 24H2. The collector records the
exact OS build; build number is authoritative.

``` powershell
.\experiments\commands\windows-11\24H2\collect.ps1
```

The collector is observational only.
